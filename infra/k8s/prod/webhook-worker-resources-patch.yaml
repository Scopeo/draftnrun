apiVersion: apps/v1
kind: Deployment
metadata:
  name: ada-webhook-worker
spec:
  template:
    spec:
      containers:
        - name: ada-webhook-worker
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              cpu: 250m
              memory: 512Mi
