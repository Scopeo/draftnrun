apiVersion: v1
kind: Service
metadata:
  name: ada-mcp
  labels:
    app: ada-mcp
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8090
      protocol: TCP
      name: http
  selector:
    app: ada-mcp
