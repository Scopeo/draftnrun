apiVersion: v1
kind: Service
metadata:
  name: ada-api
  labels:
    app: ada-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: ada-api
